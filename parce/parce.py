


class OptionParser():

    # def __init__(self):

    def add_option(self, name, required=False, argument=False):
        """Adds an option to the option parser.
        name can be a string or a tuple with two items. Do not include the
        dash(s). If two options are passed one must only be one character in
        length."""
        pass

    def parse(self, arguments_list):
        """Parses the given list of arguments and returns the results."""

    def _register_option_names(self, name):
        """Handles the given name input when adding an option to the parser.
        This method will also handle issues with the name, bad spec, taken,
        invalade, ect."""
        pass
